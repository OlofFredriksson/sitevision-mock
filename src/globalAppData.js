export default class globalAppData {
    // returns the JCR Node
    static getNode() {
        return undefined;
    }

    // returns the JCR Node identifier
    static get(nodeName) {
        return undefined;
    }
}
