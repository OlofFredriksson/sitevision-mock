/* No need to print out anything in the test cases */
export default class LogUtil {
    static log(text) {
        return undefined;
    }

    static warn(text) {
        return undefined;
    }

    static error(text) {
        return undefined;
    }

    static trace(text) {
        return undefined;
    }

    static debug(text) {
        return undefined;
    }

    static info(text) {
        return undefined;
    }
}
